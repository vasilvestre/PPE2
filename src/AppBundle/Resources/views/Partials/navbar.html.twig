<nav class="navbar navbar-light bg-faded">
    <button class="navbar-toggler hidden-sm-up" type="button" data-toggle="collapse" data-target="#navbar-header"
            aria-controls="navbar-header" aria-expanded="false" aria-label="Toggle navigation"></button>
    <div class="collapse navbar-toggleable-xs" id="navbar-header">
        <a class="navbar-brand" href="#">Navbar</a>
        <ul class="nav navbar-nav">
            <li class="nav-item
                {% if app.request.attributes.get('_route') == 'app_bundle_homepage' %}
                active
                {% endif %}">
                <a class="nav-link" href="{{ path('app_bundle_homepage') }}">Accueil</a>
            </li>
            <li class="nav-item
                {% if app.request.attributes.get('_route') == 'app_bundle_shop' %}
                active
                {% endif %}">
                <a class="nav-link" href="{{ path('app_bundle_shop') }}">Magasin</a>
            </li>
            <li class="nav-item
                {% if app.request.attributes.get('_route') == 'app_bundle_shop_promotion' %}
                active
                {% endif %}">
                <a class="nav-link" href="{{ path('app_bundle_shop_promotion') }}">Promotion</a>
            </li>
            <li class="nav-item
                {% if app.request.attributes.get('_route') == 'app_bundle_shop_promotion' %}
                active
                {% endif %}">
                <a class="nav-link" href="{{ path('fos_user_profile_edit') }}">Mon compte</a>
            </li>
        </ul>

        <form class="form-inline float-xs-right">
            {% if is_granted("IS_AUTHENTICATED_REMEMBERED") %}
                <a class="text-muted"> {{ 'layout.logged_in_as'|trans({'%username%': app.user.username|capitalize}, 'FOSUserBundle') }}</a>
            {% else %}
                <a class="text-muted"
                   href="{{ path('fos_user_security_login') }}">{{ 'layout.login'|trans({}, 'FOSUserBundle') }}</a>
            {% endif %}
            <input class="form-control" type="text" placeholder="Search">
            <button class="btn btn-outline-success" type="submit">Search</button>
        </form>
    </div>
</nav>